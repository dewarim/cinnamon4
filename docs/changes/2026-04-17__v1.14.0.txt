Changes v1.14.0:

- updated dependencies: jackson-dataformat-xml and jetty
- refactored java.util.Date to JavaDateTime; created/modified timestamps now are rendered without trailing "Z", just as
  yyyy-MM-dd'T'HH:mm:ss
- optimizations: cache superuser status: (do not load superuser status from DB on requests)
- optimizations: cache changeTriggers: (do not load all change triggers on each request)
- optimizations: latesthead: reduced code complexity when searching for correct version